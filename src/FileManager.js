'use strict';
var FileManager = (function () {
    function FileManager() {
        this.FileJSON = list(path, string);
        this.root = '../filesystem';
    }
    return FileManager;
}());
{
    return {
        name: 'test',
        path: 'path'
    };
}
